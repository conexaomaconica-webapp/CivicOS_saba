'use client';

/**
 * Kept as a compatibility boundary for imports created before Fase 1.
 * A browser draft is not an authoritative quote or contract.
 */
export default function CommercialSummaryForm() {
  return (
    <p role="status" style={{ color: 'var(--text-secondary)' }}>
      Resumo comercial indisponível até a persistência do checkout oficial.
    </p>
  );
}
